# xlsx 업로드 시 오류 발생 원인 정리

## 확인 필요 사항 (가장 먼저 할 일)
현재는 브라우저에 뜬 에러 메시지 원문(또는 개발자도구 Network 탭의 `/api/upload` 응답 `detail` 값)을 알 수 없어 코드 분석만으로 가능성이 높은 원인들을 정리했습니다. 정확한 원인을 좁히려면 다음 중 하나를 확인해 주세요.
- 브라우저 개발자도구(F12) → Network → `upload` 요청 → Response 탭의 `detail` 메시지
- 서버(`uvicorn`)를 실행 중인 터미널에 출력된 traceback(500 에러일 경우 여기 스택트레이스가 찍힘)

---

## 가능성 높은 원인

### 1. 빈 셀(NaN)이 있는 행에서 정수 변환 실패 (가능성 최상)
[app/main.py](app/main.py)의 `_save_analysis_results()`는 분석 결과를 DB에 저장하기 위해 다음처럼 강제 형변환을 합니다.
```python
stock_qty=int(row["stock_qty"]),
...
days_to_sell=int(min(row["days_to_sell"], 9999)),
```
엑셀에 `재고량, 원가, 판매가, 판매량` 중 하나라도 **빈 셀**이 있으면 pandas가 이를 `NaN`(float)으로 읽습니다. [app/analysis.py](app/analysis.py)의 음수 검증(`NON_NEGATIVE_COLUMNS`)은 `NaN < 0`이 `False`라서 걸러지지 않고 통과되며, 이후 `int(nan)`에서
```
ValueError: cannot convert float NaN to integer
```
가 발생합니다. `main.py`에서 `ValueError`는 400으로 잡히지만, 사용자에게는 "cannot convert float NaN to integer" 같은 알아보기 힘든 메시지가 그대로 노출됩니다. **가장 흔한 원인으로 추정됩니다.**

### 2. 숫자 컬럼에 문자/콤마가 섞여 있는 경우
`재고량`, `원가`, `판매가`, `판매량` 셀에 `"1,000"`처럼 천단위 콤마나 단위(`"개"`, `"원"`) 텍스트가 섞여 있으면 pandas가 해당 컬럼을 문자열(object) 타입으로 읽습니다. 이 경우 [analysis.py](app/analysis.py)의 `df["재고량"] * df["원가"]` 같은 산술 연산에서 `TypeError`가 발생하고, 500 에러(`엑셀 파싱 중 에러 발생: ...`)로 응답됩니다.

### 3. 입고일 형식을 인식하지 못하는 경우
`입고일` 컬럼이 날짜가 아닌 텍스트(예: `"2026.09.18"`, `"20260918"` 등 `pd.to_datetime`이 파싱하지 못하는 포맷)이거나 빈 셀이 있으면, [analysis.py](app/analysis.py)에서
```python
if df["입고일"].isnull().any():
    raise ValueError("입고일 데이터에 잘못된 날짜가 있습니다.")
```
가 그대로 발생해 400 에러로 응답됩니다. 사용자가 본 오류가 이 메시지라면 원인이 명확합니다.

### 4. 필수 컬럼명이 정확히 일치하지 않는 경우
`상품명, 재고량, 원가, 입고일, 판매가, 판매량` 6개 컬럼명이 **정확히** 일치해야 합니다. 컬럼명에 공백이 섞이거나(`"재고량 "`), 병합된 헤더 셀/2행 헤더 구조라면 pandas가 다른 컬럼명(`Unnamed: 0` 등)으로 읽어 `필수 컬럼이 없습니다: [...]` 400 에러가 발생합니다.

### 5. 원가가 0이면서 판매가도 없는 행
원가가 0인 행은 감가율 계산에서 0으로 안전하게 처리되지만(`np.where(df["원가"] > 0, ..., 0)`), 재고금액(`재고량 × 원가`)이 0이 되어 이후 대시보드의 "예상 절감액" 계산 등에서 의미 없는 값(0)이 나올 수 있습니다. 이 자체가 에러를 유발하진 않지만 데이터 품질 이슈로 남습니다.

---

## 결론 및 다음 조치 제안
가장 유력한 원인은 **엑셀 셀 중 하나라도 비어 있어(NaN) 정수 변환(`int()`)에서 예외가 발생**하는 케이스입니다. 확정하려면 위 "확인 필요 사항"의 에러 메시지 원문을 확인해 주시면, 그에 맞춰 다음 방어 코드를 추가하는 수정을 진행할 수 있습니다.
- 업로드 시점에 필수 컬럼의 결측치(NaN) 여부를 사전 검증하여 명확한 400 메시지로 안내
- 숫자 컬럼을 `pd.to_numeric(..., errors="coerce")`로 강제 변환 후 실패 행을 사용자에게 안내
