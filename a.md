# StockClear 긴급 코드 리뷰 수정 전/후 정리

검토 기준: 2026-09-21 현재 로컬 코드 기준

이 문서는 기존 긴급 리뷰 항목을 현재 코드 상태에 맞춰 **수정 후**, **부분 수정**, **수정 전**, **추가 수정 필요**로 다시 분류한 것입니다.

## 1. 수정 후로 분류할 수 있는 항목

### [H-1] 업로드 필수 컬럼명 오타

- **수정 전:** 필수 컬럼 목록의 판매량 항목이 `판매량`이 아니라 `판 매량`처럼 잘못 적혀 정상 파일이 거부될 수 있었습니다.
- **수정 후:** `app/main.py`의 `required_columns`가 `['상품명', '재고량', '원가', '입고일', '판매가', '판매량']`로 맞춰져 있습니다.
- **확인 내용:** `app/analysis.py`도 같은 컬럼명인 `판매량`을 기준으로 분석합니다.
- **추가로 하면 좋은 것:** 실제 샘플 CSV/XLSX 업로드 테스트를 한 번 실행해 컬럼명, 날짜, 숫자 변환까지 통합 확인해야 합니다.

### [H-4] 대시보드·재고·전략·내보내기 API 부재

- **수정 전:** 프런트엔드가 호출하는 `/api/dashboard`, `/api/inventory`, `/api/inventory/{item_id}`, `/api/strategy`, `/api/export` API가 없어 화면이 404가 될 수 있었습니다.
- **수정 후:** `app/main.py`에 위 API들이 추가되어 있고, `get_current_user`로 로그인 사용자 범위를 제한합니다.
- **확인 내용:** 각 API가 `RawInventory`와 `AnalysisResult`를 조인해 현재 사용자 데이터만 조회합니다.
- **추가로 하면 좋은 것:** 프런트엔드 화면에서 실제 응답 필드명이 맞는지 브라우저 통합 테스트가 필요합니다.

### [H-5] LLM JSON 응답 옵션 오류

- **수정 전:** OpenAI 호출의 `response_format`이 잘못되면 JSON 응답 보장이 깨지거나 요청이 실패할 수 있었습니다.
- **수정 후:** `app/llm.py`에서 `response_format={"type": "json_object"}` 형식으로 호출합니다.
- **확인 내용:** 응답 내용이 비어 있으면 실패 처리하고, `json.loads()` 후 `AIStrategy` Pydantic 모델로 검증합니다.
- **추가로 하면 좋은 것:** 실제 OpenAI API 키로 통합 호출 테스트를 한 번 수행해야 합니다.

### [H-6] LLM 원문 예외 API 노출

- **수정 전:** LLM 예외 문자열이 사용자 응답 `comment`에 그대로 들어갈 수 있었습니다.
- **수정 후:** 상세 예외는 `logger.exception()`으로 내부 로그에만 남기고, 사용자 응답에는 일반화된 메시지만 반환합니다.
- **확인 내용:** JSON 파싱 실패, Pydantic 검증 실패, API 키 누락 등은 원문 예외를 사용자에게 노출하지 않습니다.
- **추가로 하면 좋은 것:** 운영 로그에 민감정보가 과하게 남지 않도록 로그 수집 정책도 같이 점검해야 합니다.

### [H-10] AI 진단 요청 비용·호출 제한 없음

- **수정 전:** 인증된 사용자가 `/api/ai-diagnose`를 반복 호출해 OpenAI 비용과 quota를 소모할 수 있었습니다. router 자체 인증도 `main.py`의 전역 등록 방식에 의존했습니다.
- **수정 후:** `app/router.py`의 `diagnose_inventory()`가 직접 `get_current_user`를 요구합니다.
- **수정 후:** 사용자별 rate limit과 IP별 rate limit을 각각 적용합니다.
- **수정 후:** AI 요청 데이터 크기를 3,000자 이하로 제한합니다.
- **수정 후:** LLM 호출에 12초 타임아웃을 적용하고, timeout 또는 `분석 오류` 발생 시 규칙 기반 fallback을 사용합니다.
- **알아야 할 점:** 현재 rate limit은 메모리 기반이라 서버 재시작 시 기록이 초기화되고, 서버를 여러 대 띄우면 인스턴스별로 따로 제한됩니다.
- **추가로 하면 좋은 것:** 운영 환경에서는 Redis 같은 외부 저장소 기반 rate limit으로 바꾸는 것이 좋습니다.

### [H-11] 업로드 파일 크기·행 수 제한 없음

- **수정 전:** 확장자만 확인한 뒤 파일 전체를 읽어 큰 파일로 메모리 사용량과 Pandas 처리 시간이 늘어날 수 있었습니다.
- **수정 후:** `app/main.py`에 업로드 제한이 추가되었습니다.
- **수정 후 제한값:** 파일 최대 10MB, multipart 요청 본문 최대 약 11MB, 최대 50,000행, 1MB 단위 읽기.
- **수정 후 검증:** 빈 파일, 잘못된 XLSX/XLS 시그니처, 비정상 CSV, 빈 데이터 행, 행 수 초과를 차단합니다.
- **알아야 할 점:** 애플리케이션 레벨 제한은 들어갔지만, Nginx/ALB/API Gateway 같은 앞단 body size 제한은 별도로 설정해야 합니다.
- **추가로 하면 좋은 것:** 대용량 파일, 빈 파일, 잘못된 확장자, CP949 CSV, UTF-8 CSV 샘플을 만들어 업로드 테스트를 해야 합니다.

### [H-12] 업로드 예외 내부 정보 노출

- **수정 전:** 업로드 중 발생한 원본 예외 문자열이 HTTP 응답 `detail`에 그대로 들어갈 수 있었습니다.
- **수정 후:** 일반 예외는 내부 로그에만 기록하고 사용자에게는 `업로드 처리 중 오류가 발생했습니다. 잠시 후 다시 시도해주세요.` 같은 일반 메시지를 반환합니다.
- **수정 후:** `ValueError`도 사용자에게 원문을 그대로 보내지 않고 일반화된 400 응답으로 처리합니다.
- **추가로 하면 좋은 것:** 실패 이력 저장까지 항상 일관되게 남는지 DB 장애 상황을 포함해 테스트해야 합니다.

## 2. 부분 수정으로 분류할 항목

### [H-2] 업로드 후 분석 결과 저장

- **수정 전:** 업로드 성공 후 원본 재고만 저장되고 `AnalysisResult`가 저장되지 않을 수 있었습니다.
- **현재 상태:** `app/main.py`에 `_save_analysis_results()`가 추가되어 `RawInventory`와 `AnalysisResult`를 저장합니다.
- **남은 문제:** 업로드 함수에서 `_save_analysis_results()`를 먼저 호출하고, 이후 `_save_history()`와 `_save_raw_inventory()`도 다시 호출합니다.
- **위험:** 같은 업로드 데이터가 `RawInventory`와 `AnalysisResult`에 중복 저장될 수 있습니다.
- **추가 수정 필요:** 저장 함수는 하나로 통합해야 합니다. 업로드 이력 1건, 원본 재고 N건, 분석 결과 N건이 한 번만 저장되도록 정리해야 합니다.

### [H-3] 업로드 저장 단계 분리로 부분 성공 가능

- **수정 전:** 업로드 이력 저장과 원본 재고 저장이 별도 commit이라 중간 실패 시 불일치가 생길 수 있었습니다.
- **현재 상태:** 아직 여러 함수에서 각각 `db.commit()`을 수행합니다.
- **남은 문제:** `_save_analysis_results()`, `_save_history()`, `_save_raw_inventory()`가 각각 commit하므로 하나의 트랜잭션으로 묶이지 않았습니다.
- **위험:** 분석 결과는 저장됐는데 이력 저장이 실패하거나, 이력은 저장됐는데 일부 재고 저장이 실패하는 상태가 생길 수 있습니다.
- **추가 수정 필요:** `upload_and_parse_excel()`에서 전체 업로드 처리를 하나의 트랜잭션으로 묶고, helper 함수들은 `commit()`하지 않고 `db.add()`와 `db.flush()`까지만 하도록 바꿔야 합니다.

## 3. 아직 수정 전으로 남은 항목

### [H-7] Kakao OAuth 외부 호출 timeout과 오류 처리 없음

- **수정 전 상태 유지:** `requests.post()`와 `requests.get()`에 timeout이 없습니다.
- **수정 전 상태 유지:** `raise_for_status()` 또는 HTTP 상태 코드 검증이 없습니다.
- **수정 전 상태 유지:** 네트워크 예외, 비JSON 응답 예외 처리가 없습니다.
- **추가 수정 필요:** Kakao token/userinfo 요청에 timeout, HTTP 오류 처리, JSON 파싱 예외 처리, 사용자용 일반 오류 메시지를 추가해야 합니다.

### [H-8] Kakao OAuth state 검증 없음

- **수정 전 상태 유지:** `kakao_login()`에서 OAuth `state`를 생성하지 않습니다.
- **수정 전 상태 유지:** `kakao_callback()`에서 callback의 `state`를 검증하지 않습니다.
- **위험:** 로그인 CSRF가 가능합니다.
- **추가 수정 필요:** 로그인 시작 시 예측 불가능한 state를 생성해 쿠키 또는 서버 세션에 저장하고, callback에서 비교한 뒤 사용 후 폐기해야 합니다.

### [H-9] Kakao 액세스 토큰 응답 로그 출력

- **수정 전 상태 유지:** `kakao_callback()`에 `print("🔥 카카오 응답 데이터:", token_json)`가 남아 있습니다.
- **위험:** access token이나 오류 응답에 포함된 민감정보가 로그에 남을 수 있습니다.
- **추가 수정 필요:** 해당 print를 제거하고, 필요한 경우 토큰 없는 최소 정보만 구조화 로그로 남겨야 합니다.

### [H-13] 로컬 `.env` 평문 자격 증명

- **현재 확인:** 현재 작업트리 파일 검색에서는 `.env`가 보이지 않습니다.
- **주의:** 이전에 실제 secret이 로컬이나 공유된 파일에 노출된 적이 있다면, 파일을 지우는 것만으로는 해결되지 않습니다.
- **추가 수정 필요:** 노출된 DB 비밀번호, Kakao secret, 세션 secret, OpenAI key는 폐기하고 재발급해야 합니다.
- **추가 수정 필요:** 저장소에는 실제 값 없는 `.env.example`만 두고, 운영에서는 배포 환경변수나 Secret Manager를 사용해야 합니다.

## 4. 추가로 우선 수정해야 할 것

1. **Kakao 토큰 로그 제거**
   - 가장 빨리 끝낼 수 있고 보안 영향이 큽니다.
   - `print("🔥 카카오 응답 데이터:", token_json)`를 삭제해야 합니다.

2. **Kakao OAuth timeout/HTTP 오류/state 검증 추가**
   - H-7, H-8을 같이 해결하는 작업입니다.
   - 외부 장애 전파와 로그인 CSRF를 줄일 수 있습니다.

3. **업로드 저장 트랜잭션 통합**
   - H-2, H-3의 남은 핵심 문제입니다.
   - 현재는 분석 저장과 원본 저장이 중복될 가능성이 있어 우선순위가 높습니다.

4. **실제 업로드 통합 테스트 추가**
   - 정상 CSV, 정상 XLSX, 빈 파일, 10MB 초과 파일, 50,000행 초과 파일, 잘못된 날짜, 음수 값 테스트가 필요합니다.

5. **AI rate limit 운영 저장소 전환 검토**
   - 현재는 메모리 기반입니다.
   - 운영에서 서버가 여러 대라면 Redis 기반으로 바꾸는 것이 좋습니다.

6. **노출된 secret 재발급 여부 확인**
   - `.env` 파일이 현재 보이지 않더라도 과거에 공유됐거나 커밋된 secret은 폐기해야 합니다.

## 5. 현재 분류 요약

| 항목 | 현재 상태 | 메모 |
| --- | --- | --- |
| H-1 | 수정 후 | 컬럼명 `판매량` 확인됨 |
| H-2 | 부분 수정 | 분석 저장은 추가됐지만 중복 저장 가능 |
| H-3 | 수정 전/부분 수정 | 여러 commit으로 트랜잭션 미통합 |
| H-4 | 수정 후 | 주요 조회 API 추가됨 |
| H-5 | 수정 후 | OpenAI JSON response format 수정 |
| H-6 | 수정 후 | LLM 원문 예외 비공개 처리 |
| H-7 | 수정 전 | Kakao timeout/HTTP 오류 처리 없음 |
| H-8 | 수정 전 | OAuth state 검증 없음 |
| H-9 | 수정 전 | Kakao token 응답 print 남음 |
| H-10 | 수정 후 | 사용자/IP rate limit, 입력 제한, timeout, fallback 추가 |
| H-11 | 수정 후 | 파일 크기, 행 수, 빈 파일, 실제 형식 검증 추가 |
| H-12 | 수정 후 | 업로드 일반 예외 메시지 비공개 처리 |
| H-13 | 확인/조치 필요 | 현재 `.env`는 검색되지 않지만 노출 secret은 재발급 필요 |
